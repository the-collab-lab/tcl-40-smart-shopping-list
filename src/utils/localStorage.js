//this will be deleted when we merge our branches and the line below this will be uncommented out
export const userToken = JSON.parse(localStorage.getItem('item'));

//export const userToken = JSON.parse(localStorage.getItem('token'));
